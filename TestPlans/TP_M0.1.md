# TP_M0.1 — Project skeleton (Phase B)

Manual test plan for slice **M0.1 Phase B**. Run these in order from a fresh state (Unity Editor closed before starting).

Fill in `Actual:` for each step. A step passes when `Actual` matches `Expected`.

---

## Happy path

- [ ] Step 1. Open a file explorer at the repo root (`C:\Users\Matt\Desktop\Code\dungeon_debt`).
      Expected: The following exist at the repo root: `.gitignore`, `TestPlans/` (containing `.gitkeep` and `TP_M0.1.md`), `DungeonDebt/` (the Unity project), and the workflow `.md` files (`CLAUDE.md`, `GAME_DESIGN.md`, `IMPLEMENTATION_PLAN.md`, `SESSION_PROTOCOL.md`, `NEXT_SESSION.md`, `PROGRESS.md`, `REGRESSIONS.md`).
      Actual:

- [ ] Step 2. Navigate to `DungeonDebt/Assets/`.
      Expected: Folders `Scripts/`, `Prefabs/`, `Art/`, `Scenes/`, `Settings/` exist. `Scripts/` contains five subfolders: `Core`, `Data`, `Run`, `Combat`, `UI`. Each new folder (`Scripts/Core`, `Scripts/Data`, `Scripts/Run`, `Scripts/Combat`, `Scripts/UI`, `Prefabs`, `Art`) contains a `.gitkeep` file.
      Actual:

- [ ] Step 3. Launch Unity Hub and open the `DungeonDebt` project. Wait for the import to finish.
      Expected: Editor opens. Console (Window → General → Console) shows zero errors and zero warnings after the import completes. The scene `Main` is open.
      Actual:

- [ ] Step 4. In the Unity **Project** window, expand `Assets/Scripts/`.
      Expected: All five subfolders (`Core`, `Data`, `Run`, `Combat`, `UI`) appear. `Prefabs/` and `Art/` are visible under `Assets/`. Each folder is empty in the Project view (Unity hides `.gitkeep`).
      Actual:

- [ ] Step 5. With the Editor open and Main scene loaded, look at the Hierarchy.
      Expected: Exactly two root GameObjects: `Canvas` and `EventSystem`. Nothing else.
      Actual:

- [ ] Step 6. In the Unity **Project** window, confirm `Assets/Scenes/Main.unity` opens without errors when double-clicked.
      Expected: Scene reloads, Console remains clean (no new errors or warnings).
      Actual:

---

## Edge cases

- [ ] Step 7. In Unity, right-click in the Project window over `Assets/Scripts/Core` → **Show in Explorer** (or equivalent).
      Expected: Explorer opens to that folder. `.gitkeep` is present. A matching `.gitkeep.meta` file has been auto-generated by Unity.
      Actual:

- [ ] Step 8. Close Unity. Reopen the project from Unity Hub again.
      Expected: Project opens to `Main.unity`. Console still shows zero errors and zero warnings.
      Actual:

---

## Rule checks

- [ ] Step 9. In a file explorer, inspect `DungeonDebt/Assets/`.
      Expected: No folder named `Tests`, `Editor`, `Resources`, or `StreamingAssets` exists anywhere under `Assets/`. (`InputSystem_Actions.inputactions` and `Settings/` are pre-existing template files and are expected to remain.)
      Actual:

- [ ] Step 10. Search project-authored source locations for `.cs` files (`Get-ChildItem DungeonDebt\Assets,TestPlans -Recurse -Filter *.cs -Force` in PowerShell at repo root).
      Expected: Zero results. No project-authored C# source files exist yet — first code lands in M1. Ignore Unity-generated package/cache files under `DungeonDebt/Library/`.
      Actual:

- [ ] Step 11. Confirm `TestPlans/` is at the **repo root**, not under `DungeonDebt/Assets/`.
      Expected: `C:\Users\Matt\Desktop\Code\dungeon_debt\TestPlans\TP_M0.1.md` exists. No `TestPlans/` folder exists anywhere under `DungeonDebt/Assets/`.
      Actual:

- [ ] Step 12. Open `.gitignore` at the repo root in a text editor.
      Expected: It contains glob entries for at minimum: `[Ll]ibrary/`, `[Tt]emp/`, `[Oo]bj/`, `[Bb]uild/`, `[Bb]uilds/`, `[Ll]ogs/`, `[Mm]emoryCaptures/`, `[Uu]ser[Ss]ettings/`, `*.csproj`, `*.sln`, `*.suo`, `*.user`, `*.userprefs`, `*.unityproj`, `*.booproj`, `*.pidb`, `.vs/`, `.vsconfig`, `*.apk`, `*.aab`, `*.unitypackage`.
      Actual:

---

## Regression checks

- [ ] Step 13. With Unity open, switch to the **Game** view.
      Expected: Viewport is fixed at 1920×1080 (the `1080p` aspect ratio set in Phase A A6). The Canvas's reference area is visible.
      Actual:

- [ ] Step 14. Open **Edit → Project Settings → Graphics**.
      Expected: The Scriptable Render Pipeline Settings field is populated with a `UniversalRenderPipelineAsset` (Phase A A7 still holds).
      Actual:

- [ ] Step 15. Open **File → Build Profiles** (or **Build Settings**).
      Expected: Windows (Standalone) is the active platform with the Unity icon next to it. Architecture `x86_64`. Player Settings still shows default resolution 1920×1080 (Phase A A5 still holds).
      Actual:

- [ ] Step 16. Select the `Canvas` in the Hierarchy and inspect the Canvas Scaler.
      Expected: UI Scale Mode `Scale With Screen Size`, Reference Resolution 1920×1080, Match `0.5` (Phase A A4 still holds).
      Actual:

---

## Observable invariants

- [ ] Step 17. Run `git status` from the repo root in PowerShell.
      Expected: Output lists the new files (`.gitignore`, `TestPlans/...`, `DungeonDebt/Assets/Scripts/.../.gitkeep`, etc.) but does **not** list `DungeonDebt/Library/`, `DungeonDebt/Temp/`, `DungeonDebt/Logs/`, `DungeonDebt/UserSettings/`, or any `.csproj`/`.sln` files. The `.gitignore` glob entries are matching.
      Actual:

- [ ] Step 18. In Unity's Console, with all filters (Info / Warning / Error) enabled, scroll through every message after a clean reimport.
      Expected: Zero messages, or only benign Unity startup info that was already present before this slice. No new errors or warnings introduced by Phase B.
      Actual:

- [ ] Step 19. Confirm exactly seven `.gitkeep` files exist under `DungeonDebt/Assets/` and exactly one under `TestPlans/`.
      Expected: `DungeonDebt/Assets/Scripts/Core/.gitkeep`, `Scripts/Data/.gitkeep`, `Scripts/Run/.gitkeep`, `Scripts/Combat/.gitkeep`, `Scripts/UI/.gitkeep`, `Prefabs/.gitkeep`, `Art/.gitkeep`, plus `TestPlans/.gitkeep`. No others.
      Actual:

- [ ] Step 20. The Hierarchy of `Main.unity` is still exactly `Canvas` + `EventSystem` at the root — no leftover `SampleScene` debris, no extra GameObjects added by this slice.
      Expected: Two root objects only.
      Actual:

- [ ] Step 21. No file outside the plan was modified by this session. `CLAUDE.md`, `GAME_DESIGN.md`, `IMPLEMENTATION_PLAN.md`, `SESSION_PROTOCOL.md`, `NEXT_SESSION.md`, `PROGRESS.md`, `REGRESSIONS.md` are unchanged.
      Expected: `git diff` against the last commit shows changes only in the files listed in the M0.1 Phase B plan (added files only — no modifications to pre-existing tracked files).
      Actual:
